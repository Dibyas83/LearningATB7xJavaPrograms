package OOPs;
// TheTestingAcademy.comClass401ResponseA class is a group of objects which have common
// properties. A class can have some properties and functions (called methods).The class we
// have used is Main.When a class is created, no memory is allocated.
// Object is an instance of a class. Alldata members and member functions of the class can be
// accessed with thehelp of objects
// Objects are allocated memory space whenever they are created.

//inner classes - At times a class will be defined within a class if it is a subset of another class.
// The class contained within another is the inner class.src code  -  java compiler(javac)(compiler) - bytecode(0,1)(.class) - diferent jvm for diff os(memory that contains librarys)(java interpreters convert bytecode to native codes line by line.jit compiler connverts necessary bytecode to machine code) - windows(.exe),mac(dmg),linux(sh)

//jdk(has jre,jvm,javac(compiler),java intrpretor/loader,an archeiver(jar), a doc generator(javadoc),library,src.zip) - to dvelop app , jre - only to run the app , jvm(lbrarys) - to convert byte code to native code

// jvm includes - 1 ClassLoaders(loading,linking and veryfying Bytecode)
//          2 - Run time Data areas(Stack,Heap,Method area,Registers)
//          3 - Execution engine(interpreter,jit,GC)

public class classbasic {
}
